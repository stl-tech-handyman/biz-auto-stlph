# Инструкция по использованию тестовой страницы

## Проблема
Сервер отдает старую версию страницы. Нужно перезапустить сервер.

## Решение

### Вариант 1: Перезапустить сервер (рекомендуется)

1. **Остановите текущий сервер:**
   - Найдите окно PowerShell где запущен сервер
   - Нажмите `Ctrl+C`

2. **Запустите сервер заново:**
   ```powershell
   cd go
   .\start-local.ps1
   ```

3. **Откройте страницу в браузере:**
   ```
   http://localhost:8080/test-final-invoice.html
   ```

### Вариант 2: Открыть файл напрямую

1. **Откройте файл напрямую:**
   - Перейдите в папку `go`
   - Двойной клик на `test-final-invoice.html`
   - Файл откроется в браузере

2. **Примечание:** 
   - При открытии напрямую (file://) могут быть проблемы с CORS при запросах к API
   - Лучше использовать вариант 1 (через сервер)

## Проверка

После перезапуска сервера страница должна:
- ✅ Отображаться с правильными стилями
- ✅ Иметь кнопку переключения темы (🌓 Toggle Theme)
- ✅ Показывать форму для тестирования
- ✅ Работать с API на localhost:8080

## Если не работает

1. Проверьте, что сервер запущен:
   ```bash
   curl http://localhost:8080/api/health
   ```

2. Проверьте, что файл существует:
   ```bash
   ls go/test-final-invoice.html
   ```

3. Проверьте логи сервера на наличие ошибок

