# Endpoint Manager - Инструкция

## 🚀 Быстрый старт

### 1. Запуск сервера

```bash
cd go
go run ./cmd/api
```

Сервер запустится на порту **8080** (по умолчанию).

### 2. Открыть в браузере

**HTML Endpoint Manager:**
```
http://localhost:8080/
```

**JSON API:**
```
http://localhost:8080/?format=json
```

или с заголовком:
```bash
curl -H "Accept: application/json" http://localhost:8080/
```

## 🔍 Проверка работы

### Проверка через curl:

```bash
# HTML страница
curl http://localhost:8080/

# JSON ответ
curl http://localhost:8080/?format=json

# С заголовком
curl -H "Accept: application/json" http://localhost:8080/
```

### Проверка порта:

```bash
# Windows
netstat -ano | findstr :8080

# Linux/Mac
lsof -i :8080
```

## ⚠️ Решение проблем

### Порт 8080 занят

Если порт занят, убейте процесс:

```bash
# Windows
netstat -ano | findstr :8080
taskkill /PID <PID> /F

# Или используйте другой порт
PORT=8081 go run ./cmd/api
```

### Сервер не запускается

1. Проверьте, что вы в директории `go/`
2. Проверьте, что все зависимости установлены: `go mod download`
3. Проверьте логи при запуске

### Браузер не подключается

1. Убедитесь, что сервер запущен (проверьте вывод в терминале)
2. Попробуйте `127.0.0.1:8080` вместо `localhost:8080`
3. Проверьте firewall настройки
4. Попробуйте другой браузер

## 📊 Что показывает Endpoint Manager

- **Всего эндпоинтов**: 16
- **Публичных**: 9 (не требуют API Key)
- **Защищенных**: 7 (требуют API Key)

### Функции:

- 🔍 **Поиск** по пути, методу, описанию
- 🎯 **Фильтры**: Все / Публичные / С API Key / GET / POST
- 📋 **Таблица** с полной информацией о каждом endpoint
- 🎨 **Цветовое кодирование** методов и аутентификации

## 🛠️ Разработка

### Изменение порта

Установите переменную окружения:

```bash
PORT=3000 go run ./cmd/api
```

### Изменение HTML шаблона

Файл: `go/internal/http/handlers/endpoint_manager.html`

После изменений перезапустите сервер.

### Добавление новых endpoints

Обновите функцию `getEndpoints()` в `go/internal/http/handlers/root_handler.go`

## 📝 Примеры использования

### Получить JSON список endpoints:

```bash
curl http://localhost:8080/?format=json | jq
```

### Открыть в браузере:

Просто откройте `http://localhost:8080/` в любом браузере.

---

**Версия**: 1.0.0  
**Последнее обновление**: 2025-01-XX



