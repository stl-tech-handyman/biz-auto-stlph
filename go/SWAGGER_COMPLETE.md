# Swagger OpenAPI UI - Полная документация

## ✅ Что включено в Swagger UI

### Основные функции:
- ✅ **Интерактивное тестирование API** - тестируйте endpoints прямо в браузере
- ✅ **Полная документация** - все endpoints с описаниями, параметрами и примерами
- ✅ **Автоматическое добавление API ключа** - ключ добавляется в заголовки автоматически
- ✅ **Управление API ключом** - удобный интерфейс для ввода ключа
- ✅ **Фильтрация endpoints** - поиск и фильтрация по тегам
- ✅ **Deep linking** - прямые ссылки на конкретные endpoints
- ✅ **Валидация запросов** - проверка параметров перед отправкой
- ✅ **Просмотр ответов** - полные ответы сервера с форматированием
- ✅ **Поддержка всех HTTP методов** - GET, POST, PUT, DELETE, PATCH, HEAD, OPTIONS
- ✅ **Отображение времени запроса** - показывает длительность каждого запроса
- ✅ **Расширения OpenAPI** - поддержка всех расширений спецификации
- ✅ **Скачивание спецификации** - возможность скачать OpenAPI spec

### Дополнительные возможности:
- 🔍 **Поиск по API** - встроенный поиск по endpoints
- 📋 **Копирование запросов** - легко копировать cURL команды
- 🎨 **Современный UI** - красивый и удобный интерфейс
- 📱 **Адаптивный дизайн** - работает на всех устройствах

## 🚀 Как использовать

### 1. Перезапустите сервер

```powershell
# Остановите текущий сервер (Ctrl+C)
# Затем запустите заново:
cd go
.\start-local.ps1
```

### 2. Откройте Swagger UI

После перезапуска откройте в браузере:

```
http://localhost:8080/swagger
```

или

```
http://localhost:8080/swagger-ui
```

### 3. Настройте API Key

1. В правом верхнем углу введите API ключ: `test-api-key-12345`
2. Нажмите "Set & Reload"
3. Ключ будет автоматически добавляться ко всем запросам

### 4. Тестируйте API

1. Найдите нужный endpoint в списке
2. Нажмите "Try it out"
3. Заполните параметры запроса
4. Нажмите "Execute"
5. Просмотрите ответ сервера

## 📍 Доступные endpoints

### Swagger UI:
- `/swagger` - Основной Swagger UI
- `/swagger-ui` - Альтернативный путь
- `/swagger-simple` - Упрощенная версия (fallback)

### OpenAPI Spec:
- `/api/openapi.json` - OpenAPI спецификация в JSON
- `/api/openapi.yaml` - OpenAPI спецификация в YAML

## 🎯 Примеры использования

### Тестирование создания финального инвойса:

1. Откройте `/swagger`
2. Найдите `POST /api/stripe/final-invoice`
3. Нажмите "Try it out"
4. Заполните:
   ```json
   {
     "email": "test@example.com",
     "name": "Test Customer",
     "totalAmount": 1000.0,
     "depositPaid": 400.0,
     "currency": "usd",
     "description": "Final payment"
   }
   ```
5. Нажмите "Execute"
6. Просмотрите результат

### Тестирование отправки email:

1. Найдите `POST /api/email/final-invoice`
2. Нажмите "Try it out"
3. Заполните параметры
4. Выполните запрос

## 🔧 Технические детали

### Версия Swagger UI:
- Swagger UI: 5.10.3
- OpenAPI: 3.0.3

### Конфигурация:
- `deepLinking: true` - поддержка прямых ссылок
- `tryItOutEnabled: true` - включено тестирование
- `displayRequestDuration: true` - показ времени запроса
- `filter: true` - включена фильтрация
- `showExtensions: true` - показ расширений

### Безопасность:
- API ключ хранится в localStorage
- Автоматически добавляется в заголовок `X-Api-Key`
- Не отправляется на сторонние серверы

## 🐛 Решение проблем

### Swagger UI не загружается:
1. Проверьте, что сервер запущен: `curl http://localhost:8080/api/health`
2. Проверьте консоль браузера на наличие ошибок
3. Убедитесь, что файл OpenAPI существует: `ls go/docs/api/openapi-ru.yaml`

### OpenAPI spec не загружается:
1. Проверьте доступность: `curl http://localhost:8080/api/openapi.json`
2. Убедитесь, что сервер перезапущен с новым кодом
3. Проверьте логи сервера

### API ключ не работает:
1. Убедитесь, что ключ правильный: `test-api-key-12345`
2. Проверьте, что ключ установлен в переменной окружения `SERVICE_API_KEY`
3. Перезагрузите страницу после установки ключа

## 📚 Дополнительная информация

- OpenAPI спецификация: `go/docs/api/openapi-ru.yaml`
- Swagger UI файл: `go/internal/http/handlers/swagger-ui.html`
- Роутер: `go/internal/http/router.go`

